import styled from 'styled-components';

const Nav = styled('nav')({
  display: 'flex',
  alignItems: 'center',
  gap: 8,
  padding: 16,
  width: '100%',
  height: 44,
  fontWeight: 'bold',
  backgroundColor: 'whitesmoke',
  textAlign: 'center',
  boxShadow: '5px 5px 15px 5px #00000029',
});

export default Nav;
